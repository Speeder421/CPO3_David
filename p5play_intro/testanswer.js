let floor;
let ball;

function setup(){
    new Canvas(600,400);
    ball = new Sprite();
    ball.y=0;
    ball.x=
    ball.diameter=40;
    world.gravity.y=1;
}
function draw(){
    background(225);
}